# Optional local baseline

At most one non-production `BeaconAr.bacpac` may be placed here. The finite bootstrap imports it only into a provably empty Aspire-managed database and always publishes the current DACPAC afterward. External databases are never baseline-imported.
